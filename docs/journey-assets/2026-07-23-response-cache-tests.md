# Response cache verification

Command run from `backend/`:

```powershell
.\.venv\Scripts\pytest.exe
```

Result: 71 tests passed, including response-cache reuse, failure behavior, cache-key coverage, and API wiring.
